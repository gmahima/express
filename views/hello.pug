extends layout.pug
block content

  if name
    h2 welcome #{name}
  form(action='/hello', method='post')
    label enter name
     input(type='text', name='username')
    button(type='sumbit') submit
