doctype html
html(lang="en")
  head
    title flashCards
    link(rel="stylesheet", href="static/stylesheets/wow.css")
    
  body

    include includes/header.pug
    if name
      h2 welcome #{name}
      form(action='/goodbye', method='post')
        button(type='sumbit') goodbye
    block content
    include includes/footer.pug
